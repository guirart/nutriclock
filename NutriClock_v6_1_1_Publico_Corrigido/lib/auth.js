// NutriClock v6.1.1 — modo de teste público
// Esta edição é propositalmente compartilhada e não exige chave no navegador.
// NÃO use esta versão para dados privados ou para múltiplos usuários.

export function requireApiKey() {
  return null;
}
